/**
 * 卡片组件样式
 * 维海科技信息化管理平台
 * 版本: 3.0
 */

/* ============================================
   卡片组件
   ============================================ */
.card {
    background: var(--vh-card-bg);
    border-radius: 0 !important;
    box-shadow: var(--shadow-md);
    padding: var(--spacing-lg);
    border: 1px solid rgba(20, 47, 91, 0.08);
    transition: box-shadow var(--transition-base), transform var(--transition-base);
    position: relative;
}

.card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-2px);
}

.card-header {
    border-bottom: 2px solid var(--vh-primary);
    background-color: var(--vh-surface);
    padding-bottom: var(--spacing-md);
    margin-bottom: var(--spacing-md);
    border-radius: 0 !important;
}

.card-title {
    color: var(--vh-primary);
    font-weight: 600;
    font-size: var(--font-size-lg);
    margin: 0;
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
}

.card-body {
    padding: 0;
}

.card-footer {
    border-top: 1px solid var(--vh-border);
    padding-top: var(--spacing-md);
    margin-top: var(--spacing-md);
    background-color: var(--vh-surface);
    border-radius: 0 !important;
}

/* 信息卡片 */
.info-card {
    background: var(--info-card-bg);
    background-image: var(--info-card-bg-gradient);
    border-radius: 0 !important;
    box-shadow: var(--info-card-shadow);
    padding: var(--info-card-padding);
    margin-bottom: var(--info-card-margin-bottom);
    border: var(--info-card-border-width) solid var(--info-card-border-color);
    transition: all var(--info-animation-duration-base) var(--info-animation-easing);
    position: relative;
    overflow: hidden;
    /* 字体渲染优化 - 根据样式模板统一方案规定 */
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    font-feature-settings: "kern" 1, "liga" 1;
}

/* 信息卡片上移样式 - 用于表单页面紧凑布局 */
.info-card-compact {
    margin-top: -64px;
}

.info-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(180deg, var(--vh-primary) 0%, var(--vh-accent) 100%);
    opacity: 0;
    transition: opacity var(--info-animation-duration-base) var(--info-animation-easing);
}

.info-card:hover {
    box-shadow: var(--info-card-shadow-hover);
    transform: translateY(-2px);
    border-color: rgba(20, 47, 91, 0.15);
}

.info-card:hover::before {
    opacity: 1;
}

.info-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-sm);
    border-bottom: 2px solid var(--vh-primary);
}

.info-card-title {
    font-size: var(--info-title-font-size);
    font-weight: var(--info-title-font-weight);
    line-height: var(--info-title-line-height);
    color: var(--vh-primary);
    margin: 0;
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
}

.info-card-title::before {
    content: '';
    width: 4px;
    height: 20px;
    background: linear-gradient(180deg, var(--vh-primary) 0%, var(--vh-accent) 100%);
    border-radius: var(--radius-full);
}

.info-card-body {
    padding: 0;
}

.info-card-actions {
    display: flex;
    gap: var(--spacing-sm);
    justify-content: flex-end;
    margin-top: var(--spacing-md);
}

/* 信息卡片变体样式 */
.info-card-bordered {
    border-width: 2px;
    box-shadow: none;
}

.info-card-flat {
    box-shadow: none;
    border: 1px solid var(--vh-border-light);
}

.info-card-outlined {
    background: transparent;
    box-shadow: none;
    border: 2px solid var(--vh-border);
}

.info-card-elevated {
    box-shadow: var(--shadow-xl);
}

/* 信息卡片状态样式 */
.info-card-success {
    border-left: 4px solid var(--vh-success);
    background: var(--vh-success-soft);
}

.info-card-warning {
    border-left: 4px solid var(--vh-warning);
    background: var(--vh-warning-soft);
}

.info-card-danger {
    border-left: 4px solid var(--vh-error);
    background: var(--vh-error-soft);
}

.info-card-info {
    border-left: 4px solid var(--vh-info);
    background: var(--vh-info-soft);
}

/* 信息网格 */
.info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--info-grid-gap);
}

.info-grid-1 {
    grid-template-columns: 1fr;
}

.info-grid-2 {
    grid-template-columns: repeat(2, 1fr);
}

.info-grid-3 {
    grid-template-columns: repeat(3, 1fr);
}

.info-grid-4 {
    grid-template-columns: repeat(4, 1fr);
}

.info-item {
    display: flex;
    flex-direction: column;
    gap: var(--info-item-gap);
    padding: var(--info-item-padding);
    border-radius: 0 !important;
    transition: var(--info-item-transition);
    /* 字体渲染优化 - 根据样式模板统一方案规定 */
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    font-feature-settings: "kern" 1, "liga" 1;
}

.info-item:hover {
    background-color: var(--vh-surface);
}

.info-label {
    font-size: var(--info-label-font-size);
    color: var(--info-label-color);
    font-weight: var(--info-label-font-weight);
    letter-spacing: var(--info-label-letter-spacing);
    line-height: var(--info-label-line-height);
    text-transform: var(--info-label-text-transform);
    margin-bottom: var(--info-label-margin-bottom);
    /* 字体渲染优化 - 根据样式模板统一方案规定 */
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    font-feature-settings: "kern" 1, "liga" 1;
}

.info-value {
    font-size: var(--info-value-font-size);
    color: var(--info-value-color);
    font-weight: var(--info-value-font-weight);
    line-height: var(--info-value-line-height);
    word-break: var(--info-value-word-break);
    /* 字体渲染优化 - 根据样式模板统一方案规定 */
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    font-feature-settings: "kern" 1, "liga" 1;
}

.info-value-bold {
    font-weight: 600;
}

.info-value-large {
    font-size: var(--font-size-lg);
}

.info-value-muted {
    color: var(--vh-text-muted);
    font-weight: 400;
}

.info-value-empty {
    color: var(--vh-text-light);
    font-style: italic;
}

/* 数字显示优化 - 根据样式模板统一方案规定 */
.info-value-numeric {
    font-variant-numeric: tabular-nums;
    font-feature-settings: "tnum";
    letter-spacing: 0.02em;
}

.info-section-title {
    font-size: var(--info-section-title-font-size);
    font-weight: var(--info-section-title-font-weight);
    line-height: var(--info-section-title-line-height);
    color: #2c3e50;
    margin-bottom: 1.5rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid #3498db;
}

/* 表单卡片 */
.form-card {
    background: var(--vh-card-bg);
    border-radius: 0 !important;
    box-shadow: var(--shadow-md);
    padding: var(--spacing-lg);
    border: 1px solid var(--vh-border);
}

/* 表单区域（表单卡片别名，用于保持一致性） */
.form-section {
    background: var(--vh-card-bg);
    border-radius: 0 !important;
    box-shadow: var(--shadow-md);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
    border: 1px solid var(--vh-border);
}

/* 当 form-section 嵌套在 form 标签的 form-card 内时，form 作为容器，去掉自己的卡片样式，让 form-section 显示为独立卡片 */
form.form-card {
    background: transparent;
    box-shadow: none;
    border: none;
    padding: 0;
}

/* 当 form-section 嵌套在 form.form-card 内时，保持 form-section 的独立卡片样式 */
form.form-card .form-section {
    /* 保持 form-section 的独立卡片样式 */
    background: var(--vh-card-bg);
    box-shadow: var(--shadow-md);
    border: 1px solid var(--vh-border);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
}

.form-section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-sm);
    border-bottom: 2px solid var(--vh-primary);
}

.form-section-header h5 {
    margin: 0;
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--vh-text);
}

/* 修复 form-section 内 table-responsive 的对齐问题 */
.form-section .table-responsive {
    margin-left: 0;
    margin-right: 0;
    padding-left: 0;
    padding-right: 0;
}

/* 响应式调整 */
@media (max-width: 992px) {
    .form-card,
    .form-section {
        padding: var(--spacing-lg);
    }
}

@media (max-width: 768px) {
    .card {
        padding: var(--spacing-md);
        border-radius: 0;
    }
    
    .form-card,
    .form-section {
        padding: var(--spacing-md);
        border-radius: 0;
    }
}

@media (max-width: 576px) {
    .card {
        padding: var(--spacing-md);
        border-radius: 0;
        margin-bottom: var(--spacing-md);
    }
    
    .form-card,
    .form-section {
        padding: var(--spacing-md);
    }
    
    .info-card {
        padding: var(--spacing-md);
        margin-bottom: var(--spacing-md);
    }
    
    .info-card-header {
        padding: var(--spacing-sm) var(--spacing-md);
        margin: calc(-1 * var(--spacing-md)) calc(-1 * var(--spacing-md)) var(--spacing-md);
        width: calc(100% + 2 * var(--spacing-md));
        margin-left: calc(-1 * var(--spacing-md));
        margin-right: calc(-1 * var(--spacing-md));
    }
    
    .info-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-sm);
    }
    
    .info-grid-1,
    .info-grid-2,
    .info-grid-3,
    .info-grid-4 {
        grid-template-columns: 1fr;
    }
    
    .info-card-title::before {
        width: 3px;
        height: 16px;
    }
    
    .info-card-actions {
        width: 100%;
        justify-content: flex-start;
        flex-wrap: wrap;
    }
    
    .info-label {
        font-size: 11px;
        margin-bottom: 4px;
    }
    
    .info-value {
        font-size: var(--font-size-sm);
    }
    
    .info-section-title {
        font-size: var(--font-size-sm);
        margin-bottom: var(--spacing-sm);
    }
}

