{% extends "customer_management/_base.html" %}
{% load static %}

{% block cm_title %}商机列表{% endblock %}
{% block cm_subtitle %}<span class="pm-subtitle">查看和管理所有商机</span>{% endblock %}

{% block cm_actions %}
{% if can_create %}
<a href="{% url 'business_pages:opportunity_create' %}" class="list-btn list-btn-primary">
  创建商机
</a>
{% endif %}
{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{% static 'css/common.css' %}">
<link rel="stylesheet" href="{% static 'css/components/list_layout.css' %}">
<style>
    .list-page-container {
        padding: 12px !important;
        gap: 8px !important;
    }
</style>
{% endblock %}

{% block cm_content %}
<div class="list-page-container">
    <div class="list-page-header">
        <h2 class="mb-0">商机列表</h2>
        <p class="text-muted mb-0 mt-1">查看和管理所有商机</p>
    </div>
    
    <div class="row g-3 mb-3">
    <div class="col-md-2">
        <div class="info-card">
            <div class="info-card-header">
                <span class="info-card-title">商机总数</span>
            </div>
            <div class="info-card-value">{{ total_opportunities|default:0 }}</div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="info-card">
            <div class="info-card-header">
                <span class="info-card-title">活跃商机</span>
            </div>
            <div class="info-card-value">{{ active_opportunities|default:0 }}</div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="info-card">
            <div class="info-card-header">
                <span class="info-card-title">预计金额（万元）</span>
            </div>
            <div class="info-card-value">{{ total_estimated|floatformat:2|default:"0.00" }}</div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="info-card">
            <div class="info-card-header">
                <span class="info-card-title">加权金额（万元）</span>
            </div>
            <div class="info-card-value">{{ total_weighted_amount|floatformat:2|default:"0.00" }}</div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="info-card">
            <div class="info-card-header">
                <span class="info-card-title">本月新增</span>
            </div>
            <div class="info-card-value">{{ monthly_new|default:0 }}</div>
        </div>
    </div>
</div>
    
    <div class="list-page-filters card mb-3">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-md-3">
                <label class="form-label small">搜索</label>
                <input type="text" name="search" class="form-control form-control-sm" 
                       value="{{ search }}" placeholder="商机编号、名称、项目、客户">
            </div>
            <div class="col-md-2">
                <label class="form-label small">商机状态</label>
                <select name="status" class="form-select form-select-sm">
                    <option value="">全部状态</option>
                    {% for value, label in status_choices %}
                    <option value="{{ value }}" {% if status == value %}selected{% endif %}>{{ label }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label small">紧急程度</label>
                <select name="urgency" class="form-select form-select-sm">
                    <option value="">全部</option>
                    {% for value, label in urgency_choices %}
                    <option value="{{ value }}" {% if urgency == value %}selected{% endif %}>{{ label }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label small">关联客户</label>
                <select name="client_id" class="form-select form-select-sm">
                    <option value="">全部客户</option>
                    {% for client in clients %}
                    <option value="{{ client.id }}" {% if client_id == client.id|stringformat:"s" %}selected{% endif %}>{{ client.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label small">负责商务</label>
                <select name="business_manager_id" class="form-select form-select-sm">
                    <option value="">全部</option>
                    {% for manager in business_managers %}
                    <option value="{{ manager.id }}" {% if business_manager_id == manager.id|stringformat:"s" %}selected{% endif %}>{{ manager.get_full_name|default:manager.username }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-md-1">
                <label class="form-label small">&nbsp;</label>
                <button type="submit" class="btn btn-primary btn-sm w-100">筛选</button>
            </div>
            <div class="col-md-1">
                <label class="form-label small">&nbsp;</label>
                <a href="{% url 'business_pages:opportunity_management' %}" class="btn btn-outline-secondary btn-sm w-100">重置</a>
            </div>
        </form>
    </div>
</div>
    
    <div class="list-page-action-bar card mb-3">
        <div class="card-body py-2">
            <div class="d-flex justify-content-end align-items-center">
                <div>
                    <span class="text-muted small">共 {{ page_obj.paginator.count|default:0 }} 条记录</span>
                </div>
            </div>
        </div>
    </div>
    
    <div class="list-page-table card">
    <div class="card-body">
        <div class="table-responsive">
<table class="table table-hover table-sm">
    <thead>
      <tr>
        <th>商机编号</th>
        <th>商机名称</th>
        <th>关联客户</th>
        <th>负责商务</th>
        <th>商机状态</th>
        <th>预计金额（万元）</th>
        <th>成功概率</th>
        <th>加权金额（万元）</th>
        <th>紧急程度</th>
        <th>预计签约时间</th>
        <th>创建时间</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      {% for opportunity in page_obj %}
      <tr>
        <td><code>{{ opportunity.opportunity_number|default:'未编号' }}</code></td>
        <td>
          <a href="{% url 'business_pages:opportunity_detail' opportunity.id %}" class="text-decoration-none">
            {{ opportunity.name }}
          </a>
        </td>
        <td>
          <a href="{% url 'business_pages:customer_detail' opportunity.client.id %}" class="text-decoration-none">
            {{ opportunity.client.name }}
          </a>
        </td>
        <td>{{ opportunity.business_manager.get_full_name|default:opportunity.business_manager.username }}</td>
        <td>
          {% with s=opportunity.status %}
          <span class="badge bg-secondary">
            {{ opportunity.get_status_display|default:opportunity.status }}
          </span>
          {% endwith %}
        </td>
        <td style="text-align: right;">{{ opportunity.estimated_amount|floatformat:2 }}</td>
        <td style="text-align: center;">{{ opportunity.success_probability }}%</td>
        <td style="text-align: right; font-weight: 600;">{{ opportunity.weighted_amount|floatformat:2 }}</td>
        <td>
          {% if opportunity.urgency == 'very_urgent' %}
          <span style="color: #842029;">紧急</span>
          {% elif opportunity.urgency == 'urgent' %}
          <span style="color: #856404;">较急</span>
          {% else %}
          <span style="color: #666666;">一般</span>
          {% endif %}
        </td>
        <td>{{ opportunity.expected_sign_date|date:'Y-m-d'|default:'未设置' }}</td>
        <td>{{ opportunity.created_time|date:'Y-m-d H:i' }}</td>
                            <td>
                                <div class="btn-group btn-group-sm">
                                    <a href="{% url 'business_pages:opportunity_detail' opportunity.id %}" class="btn btn-outline-primary" title="查看">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    {% if can_create or opportunity.business_manager == user %}
                                    <a href="{% url 'business_pages:opportunity_edit' opportunity.id %}" class="btn btn-outline-secondary" title="编辑">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a href="{% url 'business_pages:opportunity_delete' opportunity.id %}" class="btn btn-outline-danger" title="删除" onclick="return confirm('确定要删除此商机吗？');">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                    {% endif %}
                                </div>
                            </td>
      </tr>
      {% empty %}
      <tr>
        <td colspan="12" class="text-center text-muted py-5">
                                <i class="bi bi-inbox" style="font-size: 48px; opacity: 0.3;"></i>
                                <p class="mt-3 mb-0">暂无商机数据</p>
                                <p class="text-muted small">请创建第一个商机开始使用</p>
                            </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
        </div>
        
        {% if page_obj.has_other_pages %}
        <nav aria-label="商机列表分页" class="mt-3">
            <ul class="pagination pagination-sm justify-content-end mb-0">
                {% if page_obj.has_previous %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.previous_page_number }}{% if request.GET.search %}&search={{ request.GET.search }}{% endif %}{% if request.GET.status %}&status={{ request.GET.status }}{% endif %}{% if request.GET.urgency %}&urgency={{ request.GET.urgency }}{% endif %}{% if request.GET.client_id %}&client_id={{ request.GET.client_id }}{% endif %}{% if request.GET.business_manager_id %}&business_manager_id={{ request.GET.business_manager_id }}{% endif %}{% if request.GET.expected_sign_date_from %}&expected_sign_date_from={{ request.GET.expected_sign_date_from }}{% endif %}{% if request.GET.expected_sign_date_to %}&expected_sign_date_to={{ request.GET.expected_sign_date_to }}{% endif %}">上一页</a>
                </li>
                {% else %}
                <li class="page-item disabled">
                    <a class="page-link" href="#" tabindex="-1">上一页</a>
                </li>
                {% endif %}
                
                {% for num in page_obj.paginator.page_range %}
                {% if page_obj.number == num %}
                <li class="page-item active">
                    <a class="page-link" href="#">{{ num }}</a>
                </li>
                {% elif num > page_obj.number|add:'-3' and num < page_obj.number|add:'3' %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ num }}{% if request.GET.search %}&search={{ request.GET.search }}{% endif %}{% if request.GET.status %}&status={{ request.GET.status }}{% endif %}{% if request.GET.urgency %}&urgency={{ request.GET.urgency }}{% endif %}{% if request.GET.client_id %}&client_id={{ request.GET.client_id }}{% endif %}{% if request.GET.business_manager_id %}&business_manager_id={{ request.GET.business_manager_id }}{% endif %}{% if request.GET.expected_sign_date_from %}&expected_sign_date_from={{ request.GET.expected_sign_date_from }}{% endif %}{% if request.GET.expected_sign_date_to %}&expected_sign_date_to={{ request.GET.expected_sign_date_to }}{% endif %}">{{ num }}</a>
                </li>
                {% endif %}
                {% endfor %}
                
                {% if page_obj.has_next %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.next_page_number }}{% if request.GET.search %}&search={{ request.GET.search }}{% endif %}{% if request.GET.status %}&status={{ request.GET.status }}{% endif %}{% if request.GET.urgency %}&urgency={{ request.GET.urgency }}{% endif %}{% if request.GET.client_id %}&client_id={{ request.GET.client_id }}{% endif %}{% if request.GET.business_manager_id %}&business_manager_id={{ request.GET.business_manager_id }}{% endif %}{% if request.GET.expected_sign_date_from %}&expected_sign_date_from={{ request.GET.expected_sign_date_from }}{% endif %}{% if request.GET.expected_sign_date_to %}&expected_sign_date_to={{ request.GET.expected_sign_date_to }}{% endif %}">下一页</a>
                </li>
                {% else %}
                <li class="page-item disabled">
                    <a class="page-link" href="#" tabindex="-1">下一页</a>
                </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
    </div>
</div>
</div>
{% endblock %}
